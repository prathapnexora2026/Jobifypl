# Copy this file to  android/keystore.properties  and fill in your real values.
# NEVER commit the real file or the .jks — they are git-ignored.
# storeFile is resolved relative to android/app/  (put the .jks there, or use an absolute path).
storeFile=jobifypl-upload.jks
storePassword=CHANGE_ME
keyAlias=jobifypl
keyPassword=CHANGE_ME
